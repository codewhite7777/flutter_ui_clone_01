import 'package:flutter/material.dart';

class CommonField extends StatelessWidget {
  const CommonField({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
